[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "reference_validator"
version = "0.1.0"
description = "Telecom Compliance LangGraph Agent"
dependencies = [
    "langgraph",
    "langchain",
    "pydantic"
]
